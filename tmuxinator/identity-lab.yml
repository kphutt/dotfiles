name: identity-lab
root: ~/dev/identity-lab

windows:
  - dev:
      layout: even-horizontal
      panes:
        - claude:
            - echo "🔐 Identity Lab — run 'claude' to start"
        - git:
            - git status
