name: inbox-shepherd
root: ~/dev/inbox-shepherd

windows:
  - dev:
      layout: even-horizontal
      panes:
        - claude:
            - echo "🐑 Inbox Shepherd — run 'claude' to start"
        - git:
            - git status
